import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ip_history_web',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
